var matOriginal = [];
var checkMatrix = Array(8).fill(Array(8).fill(0));

for (i = 0; i < 8; i++)
{
    matOriginal[i] = Array(8).fill(7-i);
}
function deshabilitador (checkMatrix, startingPosition){
    // functionality

    //
    if(algo)
    {
        return Number;
    }
    else{
        var mynumber += deshabilitador();
    }
};